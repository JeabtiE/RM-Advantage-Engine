# Phase 6.2 — normalization notes, actual output

Generated by calling the real normalizers in api/claude-agent.js.
No API calls, no network. Regenerate any time with the same fixtures.

## normalizeAgent1Output

- **non-array field** → `affected_sectors was not an array — reset to an empty list`
- **lowercase + dedupe** → `Lowercased affected_sectors and removed duplicates`
- **sector_impacts not an array** → `Removed sector_impacts — not an array`
- **entry with no sector** → `Dropped sector_impacts entries with no sector name`
- **unheld sector** → `Dropped sector_impacts "crypto" — not a sector the book holds`
- **duplicate sector** → `Dropped duplicate sector_impacts "property" (kept the first)`
- **duplicate sector** → `Added "property" to affected_sectors to match sector_impacts (otherwise these clients drop off the list)`
- **invalid direction** → `Invalid direction for "banking" — set to neutral`
- **reason too long** → `Truncated the reason for "banking" to 300 characters`
- **reason too long** → `Added "banking" to affected_sectors to match sector_impacts (otherwise these clients drop off the list)`
- **non-text reason** → `Removed a non-text reason for "banking"`
- **non-text reason** → `Sector "banking" (positive) has no reason — check before approving`
- **non-text reason** → `Added "banking" to affected_sectors to match sector_impacts (otherwise these clients drop off the list)`
- **missing reason** → `Sector "banking" (positive) has no reason — check before approving`
- **missing reason** → `Added "banking" to affected_sectors to match sector_impacts (otherwise these clients drop off the list)`
- **union into affected_sectors** → `Added "banking" to affected_sectors to match sector_impacts (otherwise these clients drop off the list)`
- **neutral removal** → `Removed neutral sectors from affected_sectors: energy — not used for client matching`
- **invalid event_scope** → `Removed an invalid event_scope — matching defaults to systemic`
- **event_scope case** → `Lowercased event_scope to "systemic"`
- **single_company, no held ticker** → `single_company news, but no client holds the named company — the client list is empty`

## normalizeAgent2Output

- **blank/non-text issues** → `Dropped blank or non-text flagged_issues entries`
- **blank/non-text issues** → `Set is_valid to false — 1 issue(s) flagged`
- **bare string** → `Converted flagged_issues from a single string to a list`
- **bare string** → `Set is_valid to false — 1 issue(s) flagged`
- **invalid shape** → `flagged_issues had an invalid shape — reset to an empty list`
- **invalid shape** → `Set is_valid to true — flagged_issues is empty`
- **is_valid re-derived true** → `Set is_valid to true — flagged_issues is empty`
- **is_valid re-derived false** → `Set is_valid to false — 2 issue(s) flagged`

